---
services:
    aws.greengrass.Mqtt:
        configuration:
            useTLS: 'false'
        dependencies:
            - aws.greengrass.CertificateManager
    main:
        lifecycle:
            install:
                all: echo All installed
        dependencies:
            - aws.greengrass.Mqtt
