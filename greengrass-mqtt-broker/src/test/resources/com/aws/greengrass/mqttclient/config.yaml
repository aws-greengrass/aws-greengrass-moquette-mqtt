---
services:
    aws.greengrass.Mqtt:
        parameters: {}
        dependencies:
            - aws.greengrass.CertificateManager
    main:
        lifecycle:
            install:
                all: echo All installed
        dependencies:
            - aws.greengrass.Mqtt
