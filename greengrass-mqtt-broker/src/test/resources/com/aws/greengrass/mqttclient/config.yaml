---
services:
    aws.greengrass.Mqtt:
        dependencies:
            - aws.greengrass.CertificateManager
    main:
        lifecycle:
            install:
                all: echo All installed
        dependencies:
            - aws.greengrass.Mqtt
