---
services:
    aws.greengrass.Mqtt:
        parameters:
    main:
        lifecycle:
            install:
                all: echo All installed
        dependencies:
            - aws.greengrass.Mqtt
