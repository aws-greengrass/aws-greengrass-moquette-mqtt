---
templateVersion: '2020-01-25'
componentName: aws.greengrass.mqtt.broker
description: AWS Greengrass MQTT Broker
publisher: AWS
version: '0.0.0'
componentType: plugin
manifests:
    - lifecycle:
      artifacts:
          - uri: s3://gg-dev-artifacts/$componentName/$platform/$version/aws.greengrass.mqtt.broker.jar
